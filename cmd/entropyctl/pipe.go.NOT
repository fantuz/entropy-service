package main

import (
	"io"
	"net/http"
	"os"
)

func pipe(server string) error {

	for {

		resp, err := http.Get(server)
		if err != nil {
			return err
		}

		_, err = io.Copy(os.Stdout, resp.Body)

		resp.Body.Close()

		if err != nil {
			return err
		}
	}
}
